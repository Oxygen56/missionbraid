# Iteration 6 adaptive Handoff fixture

This disposable Git repository supports two real Workbench Missions:

- `PRIMARY-SUCCESS` proves that a successful preferred Runtime reaches verification without launching declared fallback Profiles.
- `ADAPTIVE` proves that a controlled provider interruption after a real Codex workspace delta causes MissionBraid to build a digest-only Composite Checkpoint, choose an eligible fallback deterministically, and continue through a native Harness acknowledgement and Receipt.

Run the repository-level preparation and proof scripts; do not run this template in place.
